server {
    listen       80;
    listen  [::]:80;
    server_name  localhost;

    location / {
        root /asmbb;
        fastcgi_pass unix:/asmbb/engine.sock;
        include fastcgi_params;
    }
}